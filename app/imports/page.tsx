"use client";
import { useEffect, useState } from "react";
type Entry={id:string;broker:string;fileName:string;holdingCount:number;portfolioValue:number;status:string;isActive:boolean;importedAt:string;portfolio:{name:string}};
export default function ImportsPage(){const [items,setItems]=useState<Entry[]>([]);const [error,setError]=useState("");const load=()=>fetch("/api/imports").then(r=>r.json()).then(x=>setItems(x.imports??[])).catch(()=>setError("Unable to load import history."));useEffect(()=>{void load()},[]);const action=async(id:string,method:"DELETE"|"POST",suffix="")=>{const r=await fetch(`/api/imports/${id}${suffix}`,{method});if(!r.ok)setError("That action could not be completed.");void load()};return <main className="min-h-screen bg-[#f7f8f6] px-5 py-10"><div className="mx-auto max-w-5xl"><a href="/" className="text-sm font-semibold text-emerald-700">← Dashboard</a><h1 className="mt-4 text-3xl font-semibold">Import history</h1><p className="mt-2 text-sm text-slate-500">Restore a previous snapshot or download the original file.</p>{error&&<p className="mt-5 text-red-700">{error}</p>}<div className="mt-7 overflow-x-auto rounded-2xl border bg-white"><table className="w-full text-left text-sm"><thead className="border-b text-xs uppercase text-slate-500"><tr>{["Date","Portfolio","Broker","Holdings","Value","Status",""].map(x=><th className="p-4" key={x}>{x}</th>)}</tr></thead><tbody>{items.map(x=><tr className="border-b" key={x.id}><td className="p-4">{new Date(x.importedAt).toLocaleString()}</td><td className="p-4">{x.portfolio.name}</td><td className="p-4">{x.broker}</td><td className="p-4">{x.holdingCount}</td><td className="p-4">${x.portfolioValue.toLocaleString()}</td><td className="p-4">{x.isActive?"Active":"Archived"}</td><td className="flex gap-3 p-4"><a className="text-emerald-700" href={`/api/imports/${x.id}/download`}>Download</a>{!x.isActive&&<button className="text-emerald-700" onClick={()=>action(x.id,"POST","/restore")}>Restore</button>}<button className="text-red-600" onClick={()=>action(x.id,"DELETE")}>Delete</button></td></tr>)}</tbody></table></div></div></main>}
