import { holdings } from "./mock-data";
import { money, percent } from "./formatters";

const initials = (symbol: string) => symbol.slice(0, 2);

export function HoldingsTable() {
  return <section id="portfolio" className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-[0_2px_3px_rgba(15,23,42,0.02)]"><div className="flex items-center justify-between border-b border-slate-100 px-5 py-5 sm:px-6"><div><h2 className="text-lg font-semibold tracking-tight">Your holdings</h2><p className="mt-1 text-sm text-slate-500">6 positions · Mock portfolio</p></div><button className="rounded-lg border border-slate-200 px-3 py-2 text-sm font-semibold text-slate-700 transition hover:border-slate-300 hover:bg-slate-50">View all</button></div><div className="overflow-x-auto"><table className="min-w-175 w-full text-left"><thead className="border-b border-slate-100 bg-slate-50/70 text-[11px] font-mono uppercase tracking-wider text-slate-400"><tr><th className="px-5 py-3 font-medium sm:px-6">Security</th><th className="px-4 py-3 text-right font-medium">Value</th><th className="hidden px-4 py-3 text-right font-medium md:table-cell">Allocation</th><th className="px-5 py-3 text-right font-medium sm:px-6">Today</th></tr></thead><tbody>{holdings.map((holding) => <tr className="border-b border-slate-100 last:border-0" key={holding.symbol}><td className="px-5 py-4 sm:px-6"><div className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-full bg-emerald-50 text-xs font-bold text-emerald-700">{initials(holding.symbol)}</span><div><p className="font-semibold text-slate-900">{holding.symbol}</p><p className="max-w-44 truncate text-xs text-slate-500">{holding.name}</p></div></div></td><td className="px-4 py-4 text-right text-sm font-semibold text-slate-900">{money.format(holding.marketValue)}</td><td className="hidden px-4 py-4 md:table-cell"><div className="ml-auto flex w-30 items-center justify-end gap-2"><span className="h-1.5 w-16 overflow-hidden rounded-full bg-slate-100"><i className="block h-full rounded-full bg-emerald-500" style={{ width: `${holding.allocationPercent.toFixed(2)}%` }}/></span><span className="w-10 text-right text-sm text-slate-600">{percent(holding.allocationPercent)}</span></div></td><td className={`px-5 py-4 text-right text-sm font-semibold sm:px-6 ${holding.dailyChangePercent >= 0 ? "text-emerald-600" : "text-rose-600"}`}>{holding.dailyChangePercent > 0 ? "+" : ""}{percent(holding.dailyChangePercent)}</td></tr>)}</tbody></table></div></section>;
}
